public static class LevelConstants
{
    // return the final per-map index (0-based)
    public static int GetFinalLevelIndexForMap(int mapIndex)
    {
        // each map uses 5 levels (0..4) in your spec — change here if you used different count
        return 4;
    }
}
